require "spec_helper"

describe DayBreaker do
  it "has a version number" do
    expect(DayBreaker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
